@extends('layouts.smartbiz')
@section('title','Histórico de Automações')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h2 fw-bold mb-1">Execuções das automações</h1><p class="text-muted mb-0">Histórico técnico e rastreável do Workflow Engine.</p></div><a href="{{ route('automations.index') }}" class="btn btn-outline-secondary">Voltar</a></div>
<div class="card border-0 shadow-sm rounded-4"><div class="card-body"><form class="row g-2 mb-4"><div class="col-md-4"><select name="status" class="form-select"><option value="">Todos os status</option>@foreach(['completed'=>'Concluída','skipped'=>'Ignorada','failed'=>'Falhou','running'=>'Executando'] as $v=>$l)<option value="{{ $v }}" @selected($status===$v)>{{ $l }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-primary">Filtrar</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Data</th><th>Automação</th><th>Status</th><th>Registro</th><th>Detalhes</th></tr></thead><tbody>@forelse($runs as $run)<tr><td>{{ $run->executed_at?->format('d/m/Y H:i') }}</td><td>{{ $run->workflow?->name ?: 'Automação removida' }}</td><td><span class="badge {{ $run->status==='completed'?'text-bg-success':($run->status==='failed'?'text-bg-danger':'text-bg-secondary') }}">{{ $run->status }}</span></td><td>{{ class_basename($run->subject_type) }} #{{ $run->subject_id }}</td><td class="small text-muted">{{ $run->error ?: data_get($run->result,'0.type','—') }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">Nenhuma execução encontrada.</td></tr>@endforelse</tbody></table></div>{{ $runs->links() }}</div></div>
@endsection
