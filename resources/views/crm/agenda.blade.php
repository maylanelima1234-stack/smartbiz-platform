<x-app-layout>
<x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800">Agenda Comercial</h2><p class="text-sm text-gray-500">Tarefas, reuniões e follow-ups</p></div><a href="{{ route('crm.dashboard') }}" class="rounded-lg border px-4 py-2 text-sm">Dashboard</a></div></x-slot>
<div class="py-8"><div class="mx-auto max-w-6xl space-y-5 px-4 sm:px-6 lg:px-8">
<div class="flex flex-wrap gap-2">@foreach(['upcoming'=>'Próximas','today'=>'Hoje','overdue'=>'Vencidas','all'=>'Todas'] as $key=>$label)<a href="{{ route('crm.agenda',['period'=>$key]) }}" class="rounded-full px-4 py-2 text-sm {{ $period===$key ? 'bg-indigo-600 text-white' : 'bg-white text-gray-700 ring-1 ring-gray-200' }}">{{ $label }}</a>@endforeach</div>
<div class="rounded-xl bg-white shadow-sm ring-1 ring-gray-200"><div class="divide-y">@forelse($activities as $activity)<div class="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div class="flex items-center gap-2"><span class="rounded-full bg-indigo-50 px-2 py-1 text-xs text-indigo-700">{{ ucfirst($activity->type) }}</span><p class="font-medium">{{ $activity->title }}</p></div><p class="mt-1 text-sm text-gray-500"><a class="hover:underline" href="{{ route('leads.show',$activity->lead) }}">{{ $activity->lead?->name }}</a> · {{ $activity->user?->name ?? 'Sistema' }}</p></div><div class="flex items-center gap-3"><span class="text-sm {{ $activity->due_at?->isPast() ? 'text-red-600 font-semibold' : 'text-gray-500' }}">{{ $activity->due_at?->format('d/m/Y H:i') ?? 'Sem prazo' }}</span><form method="POST" action="{{ route('crm.activities.complete',$activity) }}">@csrf @method('PATCH')<button class="rounded-lg bg-emerald-600 px-3 py-2 text-xs text-white">Concluir</button></form></div></div>@empty<p class="p-8 text-center text-gray-500">Nenhuma atividade encontrada.</p>@endforelse</div></div>
{{ $activities->links() }}
</div></div>
</x-app-layout>
