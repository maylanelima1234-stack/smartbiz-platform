<x-app-layout><x-slot name="header"><div class="flex justify-between"><div><h2 class="text-xl font-semibold">Leads</h2><p class="text-sm text-gray-500">Lista comercial</p></div><a href="{{ route('crm.kanban') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm text-white">Kanban</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-200"><form method="GET" class="border-b p-4"><div class="flex gap-2"><input name="search" value="{{ $search }}" placeholder="Buscar lead" class="w-full rounded-lg border-gray-300"><button class="rounded-lg bg-gray-900 px-4 py-2 text-sm text-white">Buscar</button></div></form><div class="divide-y">@forelse($leads as $lead)<a href="{{ route('crm.leads.show',$lead) }}" class="flex justify-between p-5 hover:bg-gray-50"><div><p class="font-medium">{{ $lead->name }}</p><p class="text-sm text-gray-500">{{ $lead->email ?: $lead->phone }}</p></div><div class="text-right"><p>{{ $lead->stage?->name ?? 'Sem etapa' }}</p><p class="font-medium">R$ {{ number_format((float)$lead->value,2,',','.') }}</p></div></a>@empty<p class="p-8 text-center text-gray-500">Nenhum lead encontrado.</p>@endforelse</div><div class="border-t p-4">{{ $leads->links() }}</div></div></div></div></x-app-layout>
