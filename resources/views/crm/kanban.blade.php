<x-app-layout><x-slot name="header"><div class="flex justify-between"><div><h2 class="text-xl font-semibold">Kanban CRM</h2><p class="text-sm text-gray-500">Pipeline comercial</p></div><a href="{{ route('crm.dashboard') }}" class="rounded-lg border px-4 py-2 text-sm">Dashboard</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-full px-4 sm:px-6 lg:px-8">@forelse($pipelines as $pipeline)<h3 class="mb-4 text-lg font-semibold">{{ $pipeline->name }}</h3><div class="mb-8 flex gap-4 overflow-x-auto pb-4">@foreach($pipeline->stages as $stage)<section class="w-80 flex-none rounded-xl bg-gray-100 p-3"><div class="mb-3 flex justify-between"><div><h4 class="font-semibold">{{ $stage->name }}</h4><p class="text-xs text-gray-500">{{ $stage->leads->count() }} leads</p></div><span class="rounded-full bg-white px-2 py-1 text-xs">{{ $stage->probability }}%</span></div><div class="space-y-3">@forelse($stage->leads as $lead)<a href="{{ route('crm.leads.show',$lead) }}" class="block rounded-lg bg-white p-4 shadow-sm"><p class="font-medium">{{ $lead->name }}</p><p class="text-sm text-gray-500">{{ $lead->owner?->name ?? 'Não atribuído' }}</p><p class="mt-3 font-semibold text-emerald-600">R$ {{ number_format((float)$lead->value,2,',','.') }}</p></a>@empty<div class="rounded-lg border border-dashed p-5 text-center text-sm text-gray-400">Nenhum lead</div>@endforelse</div></section>@endforeach</div>@empty<div class="rounded-xl bg-white p-8 text-center">Nenhum pipeline configurado.</div>@endforelse</div></div></x-app-layout>
